1) не трогать оффсеты отдельно, починить так, чтобы после waverec остался адекватный оффсет (совпадающий с реальностью)

2) сделать свертку с симметрией и подумать про даунсемплинг

3) подумать про двт сразу на много уровней (без даунсемплинга, только со свертками) см скриншот

https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.oaconvolve.html
