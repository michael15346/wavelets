1) waverec(), wavedec() на несколько уровней, dwt idwt только на один

2) автоматически флипать дуальные маски

3) кортеж из всех масок одновременно

4) отрезать поля для подсчёта метрик; считать, где начинаются поляe
